function sameFrequency(arr){
    if (arr.length === 0) {
        return -1;
    }
  let count1 = 0;
  let count2 = 0;
  let pointer1 = 0;
  for (pointer2 = 1; pointer2 < arr.length; pointer2 ++) {
      if (arr[pointer1] == arr[pointer2])
  }
  
  
  
}